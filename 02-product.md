# 第二阶段提示词：完善核心能力与产品体验

你正在继续开发现有的 `Webhook Studio` 仓库。第一阶段应已实现 Endpoint、请求捕获、SQLite 持久化、SignalR 实时列表、请求详情和重放。先读取 README、项目结构、现有测试和 Git 状态；验证当前基线，不要推倒重写，也不要替换已稳定工作的技术栈。

目标是把 MVP 打磨成开发者愿意日常使用的本地工具，同时保持单体应用和有限范围。

## 1. 开始前要求

- 先运行现有测试并记录结果。如基线失败，先判断是环境问题还是已有缺陷。
- 列出本阶段会修改的文件范围和验收方法。
- 遵循现有代码风格，复用现有组件和 API 约定。
- 不做无关重构；不为了“未来扩展”创建当前没有消费者的抽象。

## 2. 本阶段新增能力

### 搜索与过滤

- Endpoint 工作台支持按 HTTP 方法、状态类别、时间范围和自由文本过滤。
- 自由文本搜索范围至少包括路径、查询字符串和可显示的文本 Body。
- 过滤由后端执行并保持分页稳定；避免一次加载全部请求。
- URL query 保存当前筛选和选中请求，使页面可刷新、可前进后退。

### 可配置响应

每个 Endpoint 可以配置捕获后返回：

- HTTP 状态码，限制在 100-599。
- Content-Type。
- 文本响应 Body，设置合理大小上限。
- 可选延迟，范围 0-10 秒。

默认值保持 `200 application/json`。配置表单应即时校验，并提供“恢复默认值”。延迟必须异步实现，不阻塞线程。

### 请求比较

- 用户可选择同一 Endpoint 下的两条请求进行比较。
- 比较方法、路径、查询、Headers 和文本/JSON Body。
- JSON 使用结构化差异，而不是只比较格式化后的字符串。
- 二进制 Body 只比较大小与摘要，不尝试渲染。
- 比较视图支持添加、删除、变化三种语义，不能只靠颜色区分。

### 导入与导出

- 单个请求可导出为 curl 和 HAR 兼容条目。
- Endpoint 的请求可导出为带版本号的 JSON 包。
- 支持导入本项目导出的 JSON 包；严格校验格式与大小，并显示导入结果。
- 不导出或重新发送 `Authorization`、`Cookie` 等敏感 Header，除非用户在明确警告后单次选择包含；默认必须脱敏。

### 数据保留

- Endpoint 可设置最多保留请求数，默认 500，允许范围 10-10000。
- 新请求保存后清理超过数量的最旧记录。
- 支持清空某 Endpoint 的全部请求，需要确认且操作后提供明确反馈。

## 3. 设计系统

这是高信息密度的实时开发者工具，不是营销型 SaaS 后台。

### 视觉方向

- 主体采用中性浅色和深色双主题，数据区保持安静。
- 强调色使用 trust teal：`#0F766E`；次级强调 `#14B8A6`；链接/可执行目标可使用 `#0369A1`；危险操作 `#DC2626`。
- 将颜色定义为语义 token，组件中不要散落原始 hex。
- 正文字体使用 IBM Plex Sans；路径、Header、JSON、状态码等技术数据使用 JetBrains Mono。
- 视觉签名是“请求脉冲轨道”：新请求进入列表时，左侧细轨出现一次短暂脉冲并停在对应请求行。它只表达实时到达，不作为装饰。
- 使用 Lucide 或项目现有 SVG 图标；禁止 emoji 图标。
- 避免大面积渐变、玻璃拟态、悬浮大圆角卡片、无意义统计图、过多阴影和巨型标题。

### 布局

桌面端采用三段式工作台：

```text
+----------------+---------------------------+----------------------------------+
| Endpoints      | Requests                  | Inspector                        |
| search/create  | filters + live stream     | summary / headers / body / replay|
+----------------+---------------------------+----------------------------------+
```

- 左栏约 240px，可折叠；中栏约 360-440px；右侧自适应。
- 1024px 以下切换为两层导航，不得产生页面级横向滚动。
- 375px 下能完成创建 Endpoint、浏览请求和重放；代码块自身可横向滚动。
- 信息密度高，但点击目标至少 44x44px，正文默认不小于 14px，关键表单不小于 16px。

### 交互与反馈

- 普通 hover/focus 反馈 100-150ms；抽屉和面板 200-300ms。
- 动画只使用 transform 和 opacity；新请求脉冲结束后不循环。
- 必须支持 `prefers-reduced-motion`，减少动态时直接插入列表并用静态标记提示。
- 实时连接状态在界面持续可见：连接中、已连接、已断开、重连失败。
- 断线期间通过普通查询恢复遗漏请求，不能假设 SignalR 永不丢消息。
- 复制、保存、重放、删除、导入均提供清楚反馈；失败消息说明原因和下一步。
- 危险操作需要确认，但不要让普通重放或复制出现多余确认。

### 无障碍

- 达到 WCAG AA 文本对比度，不能仅用绿/红表达结果。
- 三栏和 Tab 可通过键盘导航；焦点可见；打开对话框后正确管理焦点并支持 Escape。
- 图标按钮有 accessible name；实时新增内容不要抢夺用户焦点。
- 使用礼貌级别的 live region 播报关键结果，避免每次请求到达都打断屏幕阅读器。

## 4. 性能与可靠性

- 请求列表使用分页或虚拟化，避免请求数多时渲染全部 DOM。
- SignalR 更新 TanStack Query 缓存，避免每条消息触发全页面重新请求。
- 大 Body 默认折叠或截断预览，用户主动展开；不要在列表中解析完整 Body。
- 为搜索字段建立必要的 SQLite 索引；用查询计划或可解释的基准证明没有明显全表浪费。不要盲目加索引。
- 数据保留清理必须事务安全，并有并发请求测试。

## 5. 测试与验收

在保留第一阶段测试的基础上，至少增加：

- 过滤组合、稳定分页和搜索测试。
- 自定义状态码、Body、Content-Type 和异步延迟测试。
- 保留数量边界与并发捕获测试。
- 敏感 Header 默认脱敏测试。
- 导出后重新导入的一致性测试，以及非法/超大导入拒绝测试。
- JSON 结构化 Diff 测试。
- SignalR 断线恢复逻辑测试。
- 主题切换、键盘操作、对话框焦点和 reduced-motion 测试。
- Playwright 覆盖“捕获两条请求 -> 过滤 -> 比较 -> 重放”的主流程。
- 在 375、768、1024、1440 宽度进行截图或实际浏览器检查，修复溢出和遮挡。

如果仓库支持浏览器自动化，使用真实页面检查视觉结果，而不是只看 DOM 测试。

## 6. 明确不做

- 不做账号、RBAC、多租户、组织、计费。
- 不做公网隧道或托管服务。
- 不做脚本执行、工作流编排和第三方插件市场。
- 不做复杂可视化图表。
- 不更换 React、ASP.NET Core、SQLite 或 SignalR。

## 7. 完成标准

- 第一阶段全部测试仍通过。
- 搜索、过滤、自定义响应、比较、导入导出和保留策略均可实际使用。
- 实时断线后能够恢复数据，界面不会无声失效。
- 桌面和手机主流程可操作，无键盘陷阱、不可见焦点或颜色单一表达。
- UI 符合上述设计系统，而非通用模板后台。
- 没有新增无用抽象、演示数据、未使用依赖或与需求无关的重构。

完成后给出：变更摘要、关键设计决策、测试和视觉检查结果、性能考虑、仍存在的明确限制。不要隐藏失败的检查。
