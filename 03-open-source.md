# 第三阶段提示词：安全加固、工程化与开源发布

你正在完成现有 `Webhook Studio` 的开源发布版本。先读取仓库、README、前两阶段实现、测试和 Git 状态，并实际运行基线检查。目标不是增加更多产品功能，而是让现有能力安全、可部署、可维护、可贡献。

## 1. 范围原则

- 优先修复正确性、安全性、可运维性、测试和文档问题。
- 不重写架构，不新增账号系统、云服务、公网隧道、插件市场或脚本执行。
- 每项改动必须对应下面的发布要求；不要顺手格式化或重构无关文件。
- 新增依赖前解释必要性，优先使用 .NET、浏览器和现有依赖提供的能力。

## 2. 安全加固

### 重放 SSRF 防护

重放功能默认阻止：

- loopback、link-local、multicast、unspecified 和私有网络地址。
- `localhost` 及其子域表现形式。
- IPv4、IPv6、IPv4-mapped IPv6 的等价绕过。
- DNS 解析后落入受限地址的主机。
- 重定向到受限地址。
- 非 `http/https` scheme、包含用户名密码的 URL、异常端口输入。

防护必须在每次连接和每次重定向时成立，避免只校验字符串或第一次 DNS 解析。默认关闭自动重定向并以受控方式逐跳处理，设置最大重定向次数、连接超时、总超时和响应体读取上限。

考虑到这是本地开发工具，提供显式配置 `AllowPrivateNetworkReplay`，默认 `false`。用户开启时：

- UI 持续显示清晰警告。
- README 解释风险。
- 仍然禁止非 HTTP 协议、无限重定向和无限响应。

为常见绕过编写测试，测试不得访问公网。

### 捕获和展示安全

- Body 与 Header 作为数据展示，不允许注入 HTML 或脚本。
- 下载/导出文件名与 Content-Disposition 安全编码。
- 不把捕获到的 Authorization、Cookie、密钥或 Body 写入应用日志。
- 日志对 URL 查询中的常见敏感字段进行脱敏。
- 限制 Header 数量、单个 Header 长度、请求路径长度和导出大小。
- 对异常内容类型、无效 UTF-8、压缩炸弹风险和二进制 Body 给出安全处理。
- 默认仅监听 loopback；若用户显式绑定非 loopback 地址，启动日志和 UI 显示警告。
- 配置合理的安全响应头。若使用 CORS，仅允许开发所需的明确来源，不使用带凭据的通配符。

## 3. 数据与运维

- 使用正式 EF Core migrations；启动时可靠迁移，失败时停止并给出可操作错误，不能静默破坏数据库。
- SQLite 开启适当的 WAL、busy timeout 和外键约束；不要声称它适合高并发生产环境。
- 提供数据库备份与恢复说明；如果实现命令，必须保证一致性并有测试。
- 捕获 Body 的最大尺寸、保留数量、数据库路径、监听地址和私网重放开关可通过配置文件及环境变量设置，并有边界验证。
- `/health/live` 只检查进程；`/health/ready` 检查数据库可用性。健康检查不得泄露秘密和内部堆栈。
- 使用结构化日志和请求关联 ID；日志级别可配置。
- 实现优雅关闭，确保正在写入的数据不会因正常终止损坏。

## 4. Docker 与发布

提供生产可用的：

- 多阶段 Dockerfile，最终镜像不包含 SDK、Node、源码或包管理缓存。
- 非 root 用户运行。
- 固定基础镜像的大版本；说明升级策略。
- `.dockerignore`。
- Docker Compose 示例，持久化挂载 SQLite 数据目录，仅默认映射到 `127.0.0.1`。
- 容器健康检查。
- amd64 和 arm64 构建兼容性。

不要在镜像中放入示例秘密。验证镜像能够构建、启动、通过健康检查、接收请求并在重启后保留数据。

### 单进程与本地可执行程序发布

正式发布时，React 必须先构建并由 ASP.NET Core 从 `wwwroot` 托管。最终用户只启动一个 ASP.NET Core 进程，不需要单独启动前端，也不需要安装 Node.js 或 .NET Runtime。开发环境仍可分别运行 Vite 与后端以获得热更新。

除 Docker 之外，提供以下 self-contained 发布产物：

- Windows x64。
- Windows arm64。
- Linux x64。
- Linux arm64。
- macOS arm64。

发布要求：

- Windows 压缩包中提供可直接运行的 `WebhookStudio.exe`；其他平台提供对应可执行程序。
- 发布包内包含已经构建的 React 静态资源，启动时不得依赖源码目录、npm 或 Vite。
- 默认监听 `http://127.0.0.1:8080`，不得默认监听所有网络接口。
- 支持 `--open-browser` 参数：服务确认监听成功后再打开管理页面；无桌面环境、Docker 和服务器运行方式不应默认尝试打开浏览器。
- 控制台启动信息明确显示管理页面地址、Webhook 基础地址、数据库位置和停止方式。
- 数据默认写入操作系统约定的用户应用数据目录，而不是程序安装目录或当前工作目录；允许通过配置覆盖。
- 可执行文件必须能在包含空格和非 ASCII 字符的路径中运行。
- 收到 Ctrl+C、SIGTERM 或正常关闭信号时优雅停止。
- 发布压缩包使用清晰版本命名，例如 `WebhookStudio-v0.1.0-win-x64.zip`，并为每个产物生成 SHA-256 校验文件。
- README 把 Windows 双击运行、Docker 一键启动和源码开发三种方式分开说明，并明确正式使用时不需要分别启动前后端。

至少实际验证 Windows x64 发布包：

1. 在未安装 .NET Runtime、未启动 Vite 的干净环境或等价隔离环境中启动。
2. 浏览器能够加载完整页面和静态资源。
3. 能够创建 Endpoint、接收 Webhook、实时显示并完成重放。
4. 停止并重新启动后 SQLite 数据仍然存在。
5. 移动整个发布目录后仍能启动；用户数据位置保持符合文档约定。
6. `--open-browser` 生效，不传该参数时不会擅自打开浏览器。

## 5. CI 与质量门槛

创建 GitHub Actions 工作流，至少包含：

- 后端 restore/build/test。
- 前端锁文件安装、lint、typecheck、unit test、build。
- Playwright 冒烟测试。
- Docker build。
- 为各目标 RID 执行 self-contained publish，并验证前端静态资源进入发布目录。
- 依赖缓存，但不得缓存构建输出造成假通过。
- 取消同分支过时运行的 concurrency 配置。
- 最小必要 permissions。

为发布 tag 提供 GitHub Release 工作流：构建各平台压缩包、生成 SHA-256 校验值并附加到对应 Release。工作流必须支持手动触发或 tag 触发，使用最小必要权限，不向任何真实容器 registry 推送，除非仓库已有明确配置和凭据约定。

加入：

- Dependabot 或等价依赖更新配置。
- 基础静态分析和格式检查。
- 后端覆盖关键领域逻辑，前端覆盖关键交互；不要为了追求数字编写无价值测试。

## 6. 开源仓库资料

完善以下文件：

- `README.md`：提供中文主文档和可相互跳转的英文版本，包含一句话定位、截图位置、功能、快速开始、curl 示例、配置表、安全模型、架构概览、限制、路线图。
- `LICENSE`：默认使用 MIT；如果仓库已有许可证则不要擅自替换。
- `CONTRIBUTING.md`：环境、启动、测试、提交和 PR 流程。
- `CODE_OF_CONDUCT.md`。
- `SECURITY.md`：私下报告方式、支持版本、安全边界，强调捕获内容可能含秘密。
- Issue 模板：Bug、Feature。
- PR 模板：动机、变更、测试、安全与截图。
- `CHANGELOG.md`，采用 Keep a Changelog 风格。
- `docs/architecture.md`：只描述实际存在的组件、数据流和关键取舍。

README 中不要使用虚假徽章、虚假下载量、虚构性能数据、不可用链接或尚未实现的功能。示例截图如果暂时缺少，应留下明确拍摄步骤，而不是生成不存在的文件引用。

## 7. 中英文国际化

管理界面必须支持简体中文和英文即时切换，范围限定为 `zh-CN` 与 `en-US`，不为尚未要求的其他语言设计复杂扩展系统。

实现要求：

- 使用 React 生态中成熟且轻量的国际化方案，优先 `i18next` 与 `react-i18next`；如果项目已经有等价方案则沿用，不重复引入。
- 翻译资源按语言拆分为可审查的静态文件，使用语义化 key，禁止把整段 JSX 或 HTML 存入翻译字符串。
- 页面顶栏或设置菜单提供明确的“中文 / English”切换入口，不使用国旗代表语言。
- 用户首次访问时优先读取已保存选择；没有保存值时根据浏览器语言选择，中文环境使用 `zh-CN`，其他环境默认 `en-US`。
- 语言选择保存到浏览器本地存储，刷新页面、重新启动应用后保持不变；不得为了语言偏好增加账号或数据库表。
- 切换语言后当前页面、筛选条件、已选请求和未提交表单内容保持不变，不进行整页刷新。
- 更新 `<html lang>`，确保屏幕阅读器能够识别当前语言。
- 界面导航、按钮、表单、帮助文本、空状态、加载状态、确认对话框、Toast、校验错误和前端生成的错误信息必须全部覆盖两种语言。
- HTTP 方法、Header 名、URL、JSON、状态码、配置键和日志等技术数据保持原样，不翻译协议术语。
- 后端 Problem Details 使用稳定的机器可读 `code`；前端根据 `code` 本地化已知错误，未知错误可显示经过安全处理的后端 detail。不要依赖比较英文错误文本完成翻译。
- 日期、时间和数字使用当前语言的 `Intl` 格式化，同时保留可查看的 ISO 8601 原始时间；不得把已格式化文本写入数据库。
- 设计必须容纳中英文长度差异，禁止固定按钮文字宽度、截断关键操作或通过缩小到不可读字号解决溢出。
- README 的中文与英文版本必须描述相同的实际功能、启动方式、安全警告和限制，并在文档顶部互相链接。

翻译质量要求：

- 中文使用自然、简洁的开发者工具用语，不做逐词直译；同一操作在按钮、提示和文档中名称一致。
- 英文使用 sentence case 和主动语态，不保留中文式标点或语序。
- 不允许在生产界面出现翻译 key、空字符串、`undefined` 或无意的中英文混排。
- 开发环境缺失翻译时应有明显提示；发布构建不得因缺少 key 而静默显示 key 本身。

测试要求：

- 单元测试覆盖语言检测、保存偏好、缺失 key 检查和 Problem Details code 映射。
- 自动检查 `zh-CN` 与 `en-US` 的 key 集合完全一致，允许明确列出的少量例外，但不得手工忽略全部差异。
- Playwright 分别以中文和英文执行核心流程：创建 Endpoint、查看请求、过滤、重放和错误反馈。
- 验证切换语言不会丢失当前路由、选中请求、筛选条件和表单状态。
- 在 375、768、1024、1440 宽度分别抽查两种语言，确保没有文字遮挡、关键操作截断或页面级横向滚动。
- 运行无障碍检查，确认两种语言下 accessible name、焦点顺序和 `<html lang>` 正确。

## 8. API 稳定性与版本

- 为导入导出格式定义显式版本，并对未知主版本拒绝导入。
- OpenAPI 文档与真实行为一致。
- 对公开 API 生成一份基线快照或契约测试，防止意外破坏。
- 定义首个版本为 `0.1.0`，明确 pre-1.0 兼容性策略。
- 提供数据库迁移向前兼容策略；不要承诺未实现的降级能力。

## 9. 最终验证

创建并执行一份发布检查清单，至少验证：

1. 全新 clone 后按 README 启动。
2. 后端、前端、端到端测试全部通过。
3. Docker 构建和 Compose 启动成功。
4. 正式发布目录只需启动一个后端可执行程序，页面不依赖 Vite 或单独的前端进程。
5. Windows x64 self-contained 发布包按本节要求完成启动、移动目录、数据持久化和 `--open-browser` 验证。
6. 其他目标平台至少成功生成发布包，并确认包内包含可执行程序与前端静态资源；未在对应操作系统运行的必须标记为未验证。
7. curl 捕获、实时显示、过滤、比较、重放、导出导入可用。
8. 容器重启后数据仍存在。
9. 默认不能重放到 loopback、私网、IPv6 loopback 或经重定向到私网。
10. 显式启用私网重放后，本地开发场景可用且警告可见。
11. 捕获恶意 HTML/脚本时页面只显示文本，不执行。
12. 375、768、1024、1440 宽度无阻塞性布局问题。
13. 键盘导航、焦点、对比度和 reduced-motion 通过检查。
14. 中文与英文可以即时切换，刷新后保持选择，核心流程、错误信息和无障碍名称不存在缺失翻译。
15. 切换语言不会丢失当前页面、选中请求、筛选条件或表单内容，两种语言在目标宽度下均无阻塞性布局问题。
16. 中文和英文 README 内容一致且可相互跳转。
17. Git 状态中没有数据库、日志、构建目录、密钥或本机配置。

如果某项因当前环境无法运行，明确写出未验证项、原因和用户可执行的准确命令，不得以推测代替结果。

## 10. 完成输出

完成后输出一份发布候选报告，包含：

- 版本与主要能力。
- 安全加固摘要和剩余威胁边界。
- 实际执行的全部关键命令及结果。
- Docker 镜像验证结果。
- 中英文翻译完整性、双语端到端测试和响应式检查结果。
- 文档与开源治理文件列表。
- 未验证项和已知限制。
- 建议的 `v0.1.0` release notes 草稿。

只有现有功能真实可用、测试结果可信、默认配置安全、文档与代码一致时，才把它称为 release candidate。
